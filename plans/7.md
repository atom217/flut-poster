Implement poster export as image.

Tasks:
1. Add a package such as screenshot or widgets_to_image.
2. Wrap the PosterPreview so it can be rendered as an image.
3. On tapping Download, generate a PNG image.
4. Save it temporarily first.
5. Show success or error feedback with snackbar.

Requirements:
- Keep export quality high enough for social sharing.
- Ensure the exported image looks like the on-screen preview.
- Do not add share action yet.
- After coding, explain where the file is saved and any platform caveats.