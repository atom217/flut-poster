Add local template data modeling and category filtering.

Tasks:
1. Create a TemplateModel class.
2. Create dummy local data for at least 12 templates across categories:
   All, Good Morning, Good Night, Suvichar, Quotes, Birthday
3. Create a repository or local data source file.
4. When a category tab is selected, filter visible templates.
5. Show the currently selected template in the PosterPreview.
6. Add next/previous navigation or horizontal browsing for templates.

Requirements:
- Keep data local and simple.
- Do not add backend.
- Make the architecture easy to extend later with JSON or API data.
- After coding, explain how to add more templates later.